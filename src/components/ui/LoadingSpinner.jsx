import React from 'react';
import './LoadingSpinner.css';

/**
 * Componente Loading Spinner
 */
const LoadingSpinner = () => (
  <div className="loadingSpinnerBackdrop">
    <div className="loadingSpinner"></div>
  </div>
);

export default LoadingSpinner;